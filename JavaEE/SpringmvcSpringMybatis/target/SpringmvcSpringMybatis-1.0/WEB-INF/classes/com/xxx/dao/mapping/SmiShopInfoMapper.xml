<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xxx.dao.SmiShopInfoMapper">
	<resultMap id="BaseResultMap" type="SmiShopInfo">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="shop_no" property="shopNo" jdbcType="VARCHAR" />
		<result column="shop_name" property="shopName" jdbcType="VARCHAR" />
		<result column="shop_type" property="shopType" jdbcType="VARCHAR" />
		<result column="shop_password" property="shopPassword" jdbcType="VARCHAR" />
		<result column="note" property="note" jdbcType="VARCHAR" />
	</resultMap>
	<sql id="Base_Column_List">
		 id, shop_no, shop_name, shop_type, shop_password, note 
	</sql>

	<!-- 根据门店编码和密码获取门店信息 -->
	<select id="getByNoAndPwd" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from smi_shop_info
		where shop_no = #{shopNo,jdbcType=VARCHAR} and shop_password = #{password,jdbcType=VARCHAR}
	</select>

	<!-- 查询所有-->
	<select id="getAll" resultMap="BaseResultMap">
	select * from smi_shop_info
	</select>
	
	<!-- 根据id查询 -->
	<select id="getById" resultMap="BaseResultMap">
	select * from smi_shop_info where id = #{id,jdbcType=INTEGER}
	</select>
	
	<!-- 根据id删除 -->
	<delete id="delById" parameterType="Integer">
	delete from smi_shop_info where id = #{id,jdbcType=INTEGER}
	</delete>
	
	<update id="update" parameterType="com.xxx.model.SmiShopInfo">
	update from smi_shop_info set shopNo =#{shopNo:VARCHAR} , shopName = #{shopName:VARCHAR} , shopType = #{shopType:VARCHAR} , shopPassword = #{shopPassword:VARCHAR} , note= #{note:VARCHAR} where id = #{id:INTEGER}
	</update>
	
	
</mapper>
